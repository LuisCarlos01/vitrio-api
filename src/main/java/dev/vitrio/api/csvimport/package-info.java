/**
 * Importação de {@code Product} via CSV (spec 006): pré-visualização e confirmação, reusando
 * as regras de negócio de {@code product} (limite de 50, SKU único) e o pipeline de upload de
 * {@code Asset} (spec 003) pra reidratar a imagem informada por URL.
 */
package dev.vitrio.api.csvimport;
