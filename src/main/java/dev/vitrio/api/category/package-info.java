/**
 * Domínio de categoria (spec 004): agrupamento opcional de {@code Product} dentro de um
 * {@code Catalog} (CONTEXT.md), sempre isolado por catálogo (ADR-0001).
 */
package dev.vitrio.api.category;
