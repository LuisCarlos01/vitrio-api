package dev.vitrio.api.catalog;

/** Verificação é manual (spec 002, US3) — não há integração com nenhuma API real do WhatsApp. */
public enum WhatsappVerificationStatus {
    UNVERIFIED,
    VERIFIED
}
