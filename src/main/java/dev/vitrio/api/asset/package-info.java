/**
 * Domínio de upload de imagem (spec 003): normaliza e guarda no S3 uma imagem enviada por uma
 * {@code Reseller}, sempre vinculada a um único {@code Catalog} (ADR-0001).
 */
package dev.vitrio.api.asset;
