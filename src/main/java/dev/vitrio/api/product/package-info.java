/**
 * Domínio de produto (spec 004): item cadastrado dentro de um {@code Catalog} (CONTEXT.md),
 * sempre isolado por catálogo (ADR-0001), com imagem ({@code Asset}, spec 003) obrigatória e
 * categoria ({@code Category}) opcional, ambas do mesmo catálogo.
 */
package dev.vitrio.api.product;
